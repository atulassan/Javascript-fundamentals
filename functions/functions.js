// Declaring and invoking a funcitons

function demo() {
    //write some code
}

demo() // invoking a functions

//parameters and arguments

// a funciton can be defined with set of parameters that it will take parameters are generally the values that we want the functions to accept EX : function add(a, b) => a and b is a Parameters


//arguments are the values that we pass for those parameters => add()

function add(a, b) { // Parameters
    return a + b;
}

add(2, 3); // Arguments